import classes from './Leaderboard.module.css';

const Leaderboard = () => {
    return (
        <section className={classes.summary}>
            <h1>This is a placeholder for the leaderboard</h1>
        </section>
    )
}

export default Leaderboard;